interface:
  display_name: "Compose Music with LedgerLine"
  short_description: "Compose, render, mix, and revise explicit music"
  default_prompt: "Use $compose-music to establish my direction, then create and render a reviewable piece."
policy:
  allow_implicit_invocation: true
